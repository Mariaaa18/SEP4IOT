{    
    "cmake.configureSettings" : {
        "CMAKE_VERBOSE_MAKEFILE" : "ON",
        "CMAKE_BUILD_TYPE" : "Release",
        "ctest-lab.buildDirectory" : "${workspaceFolder}/target/build",
        "cmake.buildDirectory" : "${workspaceFolder}/target/build"
    }
}
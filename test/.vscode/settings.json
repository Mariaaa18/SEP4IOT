{    
    "cmake.configureSettings" : {
        "CMAKE_VERBOSE_MAKEFILE" : "ON",
    "ctest-lab.buildDirectory" : "${workspaceFolder}/test/build",
    "cmake.buildDirectory" : "${workspaceFolder}/test/build"
    }
}